import { Router } from 'express';
// import { getHotelDetails } from './../controller/hotelController';

const router = Router();

// router.get('/hotels/:id', getHotelDetails);

export default router;
